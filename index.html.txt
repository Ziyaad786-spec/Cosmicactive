```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cosmic Active Learning Centre</title>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Nunito', sans-serif;
            background-color: #0f0f2a;
            color: #ffffff;
            overflow-x: hidden;
            position: relative;
        }

        h1, h2, h3, h4, h5 {
            font-family: 'Fredoka', sans-serif;
            font-weight: 700;
        }

        .cosmic-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #1a1a3a 0%, #0f0f2a 50%, #2a1a3a 100%);
            z-index: -1;
            overflow: hidden;
            opacity: 0.6;
        }

        .star {
            position: absolute;
            background-color: white;
            border-radius: 50%;
            animation: twinkle var(--duration, 3s) infinite ease-in-out;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.1; }
            50% { opacity: 0.6; }
        }

        .planet {
            position: absolute;
            border-radius: 50%;
            box-shadow: inset 0 0 30px rgba(255, 255, 255, 0.1);
            animation: float 15s infinite ease-in-out;
            opacity: 0.6;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) translateX(0px); }
            25% { transform: translateY(-15px) translateX(8px); }
            50% { transform: translateY(7px) translateX(15px); }
            75% { transform: translateY(-7px) translateX(-12px); }
        }

        .shooting-star {
            position: absolute;
            height: 2px;
            background: linear-gradient(90deg, transparent, #a0a0ff, transparent);
            transform-origin: left;
            animation: shoot 3s linear infinite;
            opacity: 0.7;
        }

        @keyframes shoot {
            0% { transform: translateX(-100px) translateY(-100px) rotate(45deg); opacity: 0; }
            10% { opacity: 0.7; }
            90% { opacity: 0.7; }
            100% { transform: translateX(100vw) translateY(100vh) rotate(45deg); opacity: 0; }
        }

        .floating-element {
            animation: float-gentle 8s ease-in-out infinite;
        }

        @keyframes float-gentle {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .pulse {
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .bounce {
            animation: bounce 1.5s ease-in-out infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        .spin {
            animation: spin 20s linear infinite;
        }

        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header Styles */
        header {
            padding: 20px 0;
            position: relative;
            z-index: 100;
            background: rgba(15, 15, 42, 0.8);
            backdrop-filter: blur(10px);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 8s infinite linear;
            background-size: 300% 300%;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 30px;
        }

        nav a {
            color: #ffffff;
            text-decoration: none;
            font-weight: 600;
            font-size: 18px;
            transition: all 0.3s ease;
            position: relative;
        }

        nav a:hover {
            color: #ff6b6b;
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            transition: width 0.3s ease;
        }

        nav a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            padding: 100px 0;
            text-align: center;
            position: relative;
            overflow: hidden;
            background: rgba(15, 15, 42, 0.6);
            border-radius: 20px;
            margin: 30px 0;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .hero h1 {
            font-size: 4rem;
            margin-bottom: 20px;
            line-height: 1.2;
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4, #feca57);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 8s infinite linear;
            background-size: 300% 300%;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .hero p {
            font-size: 1.5rem;
            margin-bottom: 40px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.6;
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 15px 30px;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            display: inline-block;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .btn-primary {
            background: linear-gradient(45deg, #ff6b6b, #feca57);
            color: white;
            box-shadow: 0 10px 20px rgba(255, 107, 107, 0.3);
        }

        .btn-secondary {
            background: transparent;
            color: white;
            border: 3px solid #4ecdc4;
            box-shadow: 0 10px 20px rgba(78, 205, 196, 0.2);
        }

        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(255, 255, 255, 0.3);
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        /* Section Styles */
        section {
            padding: 80px 0;
            position: relative;
            z-index: 10;
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 60px;
            position: relative;
            animation: fadeInUp 1s ease-out;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 100px;
            height: 4px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            margin: 15px auto;
            border-radius: 2px;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* About Section */
        .about-content {
            display: flex;
            align-items: center;
            gap: 50px;
            flex-wrap: wrap;
            background: rgba(255, 255, 255, 0.05);
            padding: 40px;
            border-radius: 20px;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            animation: slideInLeft 1s ease-out;
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .about-text {
            flex: 1;
            min-width: 300px;
        }

        .about-text p {
            font-size: 1.2rem;
            line-height: 1.8;
            margin-bottom: 20px;
        }

        .about-icons {
            display: flex;
            gap: 30px;
            margin-top: 30px;
        }

        .icon-box {
            text-align: center;
            flex: 1;
            min-width: 120px;
            transition: all 0.3s ease;
        }

        .icon-box:hover {
            transform: translateY(-10px);
        }

        .icon-box i {
            font-size: 3rem;
            margin-bottom: 15px;
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Branches Section */
        .branches-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .branch-card {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            animation: fadeInUp 1s ease-out;
            animation-delay: calc(var(--delay, 0) * 0.2s);
        }

        .branch-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            background: rgba(255, 255, 255, 0.12);
        }

        .branch-card h3 {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #4ecdc4;
        }

        .branch-card p {
            margin-bottom: 15px;
            font-size: 1.1rem;
        }

        .contact-number {
            font-size: 1.5rem;
            font-weight: 700;
            color: #feca57;
            margin: 20px 0;
            text-align: center;
            animation: pulse 2s ease-in-out infinite;
        }

        /* Curriculum Section */
        .curriculum-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .curriculum-card {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            animation: bounceIn 1s ease-out;
            animation-delay: calc(var(--delay, 0) * 0.2s);
        }

        @keyframes bounceIn {
            0% {
                opacity: 0;
                transform: scale(0.3);
            }
            50% {
                transform: scale(1.05);
            }
            70% {
                transform: scale(0.9);
            }
            100% {
                opacity: 1;
                transform: scale(1);
            }
        }

        .curriculum-card:hover {
            transform: scale(1.05);
            background: rgba(255, 255, 255, 0.12);
        }

        .curriculum-card h3 {
            font-size: 1.8rem;
            margin-bottom: 15px;
            color: #ff6b6b;
        }

        .curriculum-card p {
            font-size: 1.1rem;
            line-height: 1.6;
        }

        /* Services Section */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .service-card {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            animation: slideInUp 1s ease-out;
            animation-delay: calc(var(--delay, 0) * 0.2s);
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .service-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.12);
        }

        .service-card i {
            font-size: 3rem;
            margin-bottom: 20px;
            color: #96ceb4;
        }

        .service-card h3 {
            font-size: 1.8rem;
            margin-bottom: 15px;
            color: #96ceb4;
        }

        /* Why Choose Section */
        .why-choose-list {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.05);
            padding: 40px;
            border-radius: 20px;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .why-choose-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 30px;
            gap: 20px;
            animation: fadeIn 1s ease-out;
            animation-delay: calc(var(--delay, 0) * 0.3s);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .why-choose-item i {
            font-size: 1.5rem;
            color: #feca57;
            margin-top: 5px;
        }

        .why-choose-text h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #4ecdc4;
        }

        /* Gallery Section */
        .gallery-container {
            max-width: 1000px;
            margin: 0 auto;
            overflow: hidden;
            border-radius: 20px;
            position: relative;
            background: rgba(255, 255, 255, 0.05);
            padding: 20px;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .gallery-slide {
            display: none;
            animation: fadeIn 1s ease-out;
        }

        .gallery-slide.active {
            display: block;
        }

        .gallery-img {
            width: 100%;
            height: 400px;
            object-fit: cover;
            border-radius: 20px;
        }

        .gallery-nav {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 30px;
        }

        .gallery-dot {
            width: 15px;
            height: 15px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .gallery-dot.active {
            background: #4ecdc4;
            transform: scale(1.2);
        }

        /* Contact Section */
        .contact-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 50px;
        }

        .contact-info {
            background: rgba(255, 255, 255, 0.05);
            padding: 30px;
            border-radius: 20px;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            animation: slideInLeft 1s ease-out;
        }

        .contact-info h3 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #ff6b6b;
        }

        .contact-detail {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
        }

        .contact-detail i {
            font-size: 1.5rem;
            color: #4ecdc4;
        }

        .social-icons {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }

        .social-icons a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            font-size: 1.5rem;
            transition: all 0.3s ease;
            animation: bounce 2s ease-in-out infinite;
            animation-delay: calc(var(--delay, 0) * 0.5s);
        }

        .social-icons a:hover {
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
            transform: translateY(-5px);
        }

        .contact-form {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            padding: 30px;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            animation: slideInRight 1s ease-out;
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 15px;
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            background: rgba(0, 0, 0, 0.2);
            color: white;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #4ecdc4;
            box-shadow: 0 0 10px rgba(78, 205, 196, 0.3);
        }

        .form-group textarea {
            height: 120px;
            resize: vertical;
        }

        /* Footer */
        footer {
            background: rgba(0, 0, 0, 0.2);
            padding: 50px 0 20px;
            margin-top: 80px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-section h3 {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #4ecdc4;
        }

        .footer-section p,
        .footer-section a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            line-height: 1.8;
            display: block;
            margin-bottom: 10px;
            transition: all 0.3s ease;
        }

        .footer-section a:hover {
            color: #4ecdc4;
            transform: translateX(5px);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.5);
        }

        .quick-links {
            display: flex;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }

            .hero p {
                font-size: 1.2rem;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 300px;
            }

            nav ul {
                gap: 15px;
                flex-wrap: wrap;
                justify-content: center;
            }

            .about-content {
                flex-direction: column;
            }

            .section-title {
                font-size: 2rem;
            }
        }

        @media (max-width: 480px) {
            .header-content {
                flex-direction: column;
                gap: 20px;
            }

            .hero {
                padding: 60px 0;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .section-title {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <!-- Background Elements -->
    <div class="cosmic-bg" id="cosmic-bg"></div>

    <!-- Header -->
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo floating-element">COSMIC ACTIVE</div>
                <nav>
                    <ul>
                        <li><a href="#home">Home</a></li>
                        <li><a href="#about">About</a></li>
                        <li><a href="#branches">Branches</a></li>
                        <li><a href="#curriculum">Curriculum</a></li>
                        <li><a href="#services">Services</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="container">
            <h1 class="pulse">Helping Every Child Shine Bright Like a Star</h1>
            <p>Cosmic Active Learning Centre empowers neurodiverse kids from Grade R to 12 with tailored education, aftercare, and extra lessons.</p>
            <div class="cta-buttons">
                <a href="#contact" class="btn btn-primary">Book a Visit</a>
                <a href="#contact" class="btn btn-secondary">Contact Us Today</a>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about">
        <div class="container">
            <h2 class="section-title">About Us</h2>
            <div class="about-content">
                <div class="about-text">
                    <p>We create a safe, supportive space where every learner can thrive. Our cosmic approach means no child is left behind. At Cosmic Active Learning Centre, we understand that each child is unique and deserves an education that caters to their individual needs.</p>
                    <p>Our dedicated team of educators specializes in supporting neurodiverse learners, providing them with the tools, confidence, and skills they need to succeed academically and personally.</p>
                    <div class="about-icons">
                        <div class="icon-box floating-element">
                            <i class="fas fa-rocket"></i>
                            <h3>Inspiring</h3>
                        </div>
                        <div class="icon-box floating-element" style="animation-delay: 0.5s;">
                            <i class="fas fa-globe-africa"></i>
                            <h3>Inclusive</h3>
                        </div>
                        <div class="icon-box floating-element" style="animation-delay: 1s;">
                            <i class="fas fa-star"></i>
                            <h3>Excellence</h3>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Branches Section -->
    <section id="branches">
        <div class="container">
            <h2 class="section-title">Our Branches</h2>
            <div class="branches-grid">
                <div class="branch-card" style="--delay: 0;">
                    <h3>Greenfields Branch</h3>
                    <p>3 Allby Road, Greenfields, East London</p>
                    <p><a href="https://maps.google.com" target="_blank" style="color: #4ecdc4;">View on Google Maps</a></p>
                </div>
                <div class="branch-card" style="--delay: 1;">
                    <h3>Cambridge Branch</h3>
                    <p>62 Weare Road, Cambridge West, East London</p>
                    <p><a href="https://maps.google.com" target="_blank" style="color: #4ecdc4;">View on Google Maps</a></p>
                </div>
            </div>
            <div class="contact-number">📞 083 739 1897</div>
        </div>
    </section>

    <!-- Curriculum Section -->
    <section id="curriculum">
        <div class="container">
            <h2 class="section-title">Our Curriculum</h2>
            <p style="text-align: center; font-size: 1.2rem; margin-bottom: 40px;">We adapt multiple curricula to meet each child's unique needs.</p>
            <div class="curriculum-grid">
                <div class="curriculum-card" style="--delay: 0;">
                    <h3>Mindscape</h3>
                    <p>A holistic approach focusing on cognitive development and emotional intelligence for neurodiverse learners.</p>
                </div>
                <div class="curriculum-card" style="--delay: 1;">
                    <h3>OPSA</h3>
                    <p>Outcomes-based learning framework designed to support diverse learning styles and abilities.</p>
                </div>
                <div class="curriculum-card" style="--delay: 2;">
                    <h3>CAPS</h3>
                    <p>South Africa's national curriculum, adapted to ensure accessibility for all learners.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Services Section -->
    <section id="services">
        <div class="container">
            <h2 class="section-title">Our Services</h2>
            <div class="services-grid">
                <div class="service-card" style="--delay: 0;">
                    <i class="fas fa-graduation-cap"></i>
                    <h3>Grades R–12</h3>
                    <p>Comprehensive main schooling with tailored support for neurodiverse learners.</p>
                </div>
                <div class="service-card" style="--delay: 1;">
                    <i class="fas fa-home"></i>
                    <h3>Aftercare</h3>
                    <p>Homework support and safe environment for continued learning after school hours.</p>
                </div>
                <div class="service-card" style="--delay: 2;">
                    <i class="fas fa-chalkboard-teacher"></i>
                    <h3>Extra Lessons</h3>
                    <p>Individual tutoring sessions focused on academic improvement and skill development.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Why Choose Section -->
    <section>
        <div class="container">
            <h2 class="section-title">Why Choose Cosmic?</h2>
            <div class="why-choose-list">
                <div class="why-choose-item" style="--delay: 0;">
                    <i class="fas fa-check-circle"></i>
                    <div class="why-choose-text">
                        <h3>Tailored Support</h3>
                        <p>Specialized programs designed specifically for neurodiverse learners with individual attention.</p>
                    </div>
                </div>
                <div class="why-choose-item" style="--delay: 1;">
                    <i class="fas fa-check-circle"></i>
                    <div class="why-choose-text">
                        <h3>Small Classes</h3>
                        <p>Intimate learning environments with small class sizes for maximum personal attention.</p>
                    </div>
                </div>
                <div class="why-choose-item" style="--delay: 2;">
                    <i class="fas fa-check-circle"></i>
                    <div class="why-choose-text">
                        <h3>Creative Environment</h3>
                        <p>Fun, structured, and engaging learning spaces that inspire curiosity and growth.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Gallery Section -->
    <section>
        <div class="container">
            <h2 class="section-title">Our Gallery</h2>
            <div class="gallery-container">
                <div class="gallery-slide active">
                    <img src="https://placehold.co/800x400/4ecdc4/ffffff?text=Happy+Students" alt="Happy Students" class="gallery-img">
                </div>
                <div class="gallery-slide">
                    <img src="https://placehold.co/800x400/ff6b6b/ffffff?text=Space+Classroom" alt="Space Classroom" class="gallery-img">
                </div>
                <div class="gallery-slide">
                    <img src="https://placehold.co/800x400/45b7d1/ffffff?text=Learning+Activity" alt="Learning Activity" class="gallery-img">
                </div>
            </div>
            <div class="gallery-nav">
                <div class="gallery-dot active" data-slide="0"></div>
                <div class="gallery-dot" data-slide="1"></div>
                <div class="gallery-dot" data-slide="2"></div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <div class="container">
            <h2 class="section-title">Get In Touch</h2>
            <div class="contact-content">
                <div class="contact-info">
                    <h3>Contact Information</h3>
                    <div class="contact-detail">
                        <i class="fas fa-phone"></i>
                        <span>083 739 1897</span>
                    </div>
                    <div class="contact-detail">
                        <i class="fas fa-envelope"></i>
                        <span>info@cosmicactive.co.za</span>
                    </div>
                    <div class="contact-detail">
                        <i class="fas fa-map-marker-alt"></i>
                        <span>Greenfields Branch: 3 Allby Road, Greenfields, East London</span>
                    </div>
                    <div class="contact-detail">
                        <i class="fas fa-map-marker-alt"></i>
                        <span>Cambridge Branch: 62 Weare Road, Cambridge West, East London</span>
                    </div>
                    <div class="social-icons">
                        <a href="#" style="--delay: 0;"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" style="--delay: 1;"><i class="fab fa-instagram"></i></a>
                    </div>
                    <a href="#contact" class="btn btn-primary" style="margin-top: 30px;">Schedule a Visit</a>
                </div>
                <div class="contact-form">
                    <form id="enquiryForm">
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" id="name" required>
                        </div>
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="email" id="email" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Phone</label>
                            <input type="tel" id="phone">
                        </div>
                        <div class="form-group">
                            <label for="message">Message</label>
                            <textarea id="message" required></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary" style="width: 100%;">Send Enquiry</button>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>Cosmic Active Learning Centre</h3>
                    <p>Empowering neurodiverse learners from Grade R to 12 with tailored education, aftercare, and extra lessons.</p>
                </div>
                <div class="footer-section">
                    <h3>Our Branches</h3>
                    <p>Greenfields Branch: 3 Allby Road, Greenfields, East London</p>
                    <p>Cambridge Branch: 62 Weare Road, Cambridge West, East London</p>
                    <p>📞 083 739 1897</p>
                </div>
                <div class="footer-section">
                    <h3>Quick Links</h3>
                    <a href="#home">Home</a>
                    <a href="#about">About Us</a>
                    <a href="#curriculum">Curriculum</a>
                    <a href="#contact">Contact</a>
                </div>
            </div>
            <div class="quick-links">
                <a href="#home">Home</a>
                <a href="#about">About</a>
                <a href="#branches">Branches</a>
                <a href="#curriculum">Curriculum</a>
                <a href="#services">Services</a>
                <a href="#contact">Contact</a>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2025 Cosmic Active Learning Centre. All Rights Reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // Create cosmic background elements
        document.addEventListener('DOMContentLoaded', function() {
            const cosmicBg = document.getElementById('cosmic-bg');
            
            // Create stars
            for (let i = 0; i < 100; i++) {
                const star = document.createElement('div');
                star.classList.add('star');
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.width = Math.random() * 2 + 1 + 'px';
                star.style.height = star.style.width;
                star.style.setProperty('--duration', (Math.random() * 3 + 2) + 's');
                cosmicBg.appendChild(star);
            }
            
            // Create planets
            const planetColors = ['rgba(255, 107, 107, 0.3)', 'rgba(78, 205, 196, 0.3)', 'rgba(69, 183, 209, 0.3)', 'rgba(150, 206, 180, 0.3)', 'rgba(254, 202, 87, 0.3)'];
            for (let i = 0; i < 3; i++) {
                const planet = document.createElement('div');
                planet.classList.add('planet');
                planet.style.left = Math.random() * 100 + '%';
                planet.style.top = Math.random() * 100 + '%';
                planet.style.width = Math.random() * 80 + 40 + 'px';
                planet.style.height = planet.style.width;
                planet.style.background = planetColors[i];
                planet.style.setProperty('--duration', (Math.random() * 10 + 10) + 's');
                cosmicBg.appendChild(planet);
            }
            
            // Create shooting stars - increased to 5
            function createShootingStar() {
                const shootingStar = document.createElement('div');
                shootingStar.classList.add('shooting-star');
                shootingStar.style.top = Math.random() * 50 + '%';
                shootingStar.style.width = Math.random() * 80 + 40 + 'px';
                shootingStar.style.left = Math.random() * 20 + '%'; // Start from left side
                cosmicBg.appendChild(shootingStar);
                
                setTimeout(() => {
                    shootingStar.remove();
                }, 3000);
            }
            
            // Create initial shooting stars
            for (let i = 0; i < 3; i++) {
                setTimeout(() => {
                    createShootingStar();
                }, i * 1000);
            }
            
            // Continuous shooting stars
            setInterval(() => {
                if (Math.random() > 0.3) { // Increased frequency
                    createShootingStar();
                }
            }, 2000);
            
            // Gallery slider
            const slides = document.querySelectorAll('.gallery-slide');
            const dots = document.querySelectorAll('.gallery-dot');
            let currentSlide = 0;
            
            function showSlide(n) {
                slides.forEach(slide => slide.classList.remove('active'));
                dots.forEach(dot => dot.classList.remove('active'));
                
                slides[n].classList.add('active');
                dots[n].classList.add('active');
                currentSlide = n;
            }
            
            dots.forEach((dot, index) => {
                dot.addEventListener('click', () => {
                    showSlide(index);
                });
            });
            
            // Auto slide
            setInterval(() => {
                let nextSlide = (currentSlide + 1) % slides.length;
                showSlide(nextSlide);
            }, 5000);
            
            // Form submission
            const form = document.getElementById('enquiryForm');
            if (form) {
                form.addEventListener('submit', function(e) {
                    e.preventDefault();
                    alert('Thank you for your enquiry! We will contact you soon.');
                    form.reset();
                });
            }
            
            // Smooth scrolling for navigation links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        window.scrollTo({
                            top: target.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                });
            });
            
            // Intersection Observer for animations
            const observerOptions = {
                threshold: 0.1
            };
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animationPlayState = 'running';
                    }
                });
            }, observerOptions);
            
            // Observe elements that should animate when they come into view
            document.querySelectorAll('.section-title, .about-content, .branch-card, .curriculum-card, .service-card, .why-choose-item, .contact-info, .contact-form').forEach(el => {
                el.style.animationPlayState = 'paused';
                observer.observe(el);
            });
        });
    </script>
</body>
</html>
```